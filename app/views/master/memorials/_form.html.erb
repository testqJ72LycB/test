<%= form_for(master_memorial) do |f| %>
  <% if master_memorial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(master_memorial.errors.count, "error") %> prohibited this master_memorial from being saved:</h2>

      <ul>
      <% master_memorial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
